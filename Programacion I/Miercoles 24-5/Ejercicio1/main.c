#include <stdio.h>
#include <stdlib.h>

// sprintf(VARIABLE DONDE ALMACENA, LO QUE SE ALMACENA);
/*
    rewind(bin);
    while(!feof(bin))
    {
        fread();
        if(codigo==prod.cod)
        {
            printf();
            precio
            scanf("%f",&prod.cod);
            fflush(stdin);
            fseek(ARCHIVO,(-1)*sizeof(ESTRUCTURA),SEEK_CURL);
            fwrite(&ESTRUCTURA,sizeof(ESTRUCTURA),CANTIDAD A MODIFICAR,ARCHIVO);
        }
    }



*/


int main()
{
    printf("Hello world!\n");
    return 0;
}
